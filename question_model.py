# Define a class named Question
class Question():
    # Define a constructor method that initializes the object with input text and answer
    def __init__(self, input_text, input_answer):
        # Assign the input text to the 'text' attribute of the object
        self.text = input_text
        # Assign the input answer to the 'answer' attribute of the object
        self.answer = input_answer

